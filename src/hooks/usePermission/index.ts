/**
 * usePermission Hook 统一导出
 */

// 导出 Hook 实现
export { usePermission, usePermissions } from './usePermission';

// 导出类型定义
export type {
  IUsePermissionSingleOptions,
  IUsePermissionSingleReturn,
  IUsePermissionMultipleOptions,
  IUsePermissionMultipleReturn,
  IUsePermissionOptions,
  IUsePermissionReturn,
} from './types';
