/**
 * 全局提示框组件统一导出
 */

export { GlobalAlert } from './GlobalAlert';
export { useGlobalAlertStore } from './store';
export { showGlobalAlert, hideGlobalAlert, showAlert, showJsonAlert } from './utils';
export type { IGlobalAlertOptions, AlertContentType } from './types';

